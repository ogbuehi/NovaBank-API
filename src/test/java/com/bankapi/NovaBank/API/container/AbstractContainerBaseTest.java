package com.bankapi.NovaBank.API.container;

/**
 * Common base class for database-backed tests.
 *
 * <p>The {@code test} profile uses an in-memory H2 database, allowing the
 * suite to run in local and CI environments that do not have Docker.</p>
 */
public abstract class AbstractContainerBaseTest {
}
